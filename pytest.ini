[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v

# Display summary output
# To see output from print statements during tests, run with: pytest -v --capture=no
filterwarnings =
    ignore::DeprecationWarning

# Configure PySpark logging to reduce verbosity
log_cli = false 